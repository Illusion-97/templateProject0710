import { AbstractFormGroupComponent } from './abstract-form-group-component';

describe('AbstractFormGroupComponent', () => {
  it('should create an instance', () => {
    expect(new AbstractFormGroupComponent()).toBeTruthy();
  });
});

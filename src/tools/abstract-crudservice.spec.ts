import { AbstractCRUDService } from './abstract-crudservice';

describe('AbstractCRUDService', () => {
  it('should create an instance', () => {
    expect(new AbstractCRUDService()).toBeTruthy();
  });
});
